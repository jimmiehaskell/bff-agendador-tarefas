spring:
  application:
    name: bff-agendador
  cloud:
    openfeign:
      httpclient:
        hc5:
          enabled: true

usuario:
  url: localhost:8080/usuario
  email:
  senha:

agendador-tarefa:
  url: localhost:8081/tarefas

notificacao:
  url: localhost:8082/email

# Roda a cada 5 minutos, todos os dias.
cron:
  horario: 0 0/5 * * * ?

server:
  port: 8083